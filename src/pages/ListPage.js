import React from 'react';
import LoadData from '../components/cards/LoadData';

const ListPage = () => {
    return (
        <div>
            <LoadData/>
        </div>
    );
};

export default ListPage;